<Window x:Class="MultiDesktopAdaptor.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Multi-Desktop Adaptor - Configuration"
        MinHeight="400" MinWidth="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Multi-Desktop Adaptor"
                   FontSize="18" FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <TabControl Grid.Row="1">
            <TabItem Header="Follow Rules">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ListView x:Name="RuleListView" MouseDoubleClick="OnRuleDoubleClick">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Window Title" Width="200"
                                                DisplayMemberBinding="{Binding WindowTitle}" />
                                <GridViewColumn Header="Class" Width="160"
                                                DisplayMemberBinding="{Binding WindowClassName}" />
                                <GridViewColumn Header="Process" Width="150"
                                                DisplayMemberBinding="{Binding ProcessName}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="+ Add Rule" Click="OnAddRuleClick" />
                        <Button Content="Remove" Click="OnRemoveRuleClick" Margin="8,0,0,0" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Commands">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" MinWidth="120" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Left: desktop selector -->
                    <ListBox x:Name="DesktopSelector" Grid.Column="0"
                             SelectionChanged="OnDesktopSelectionChanged" />

                    <!-- Splitter -->
                    <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center"
                                  VerticalAlignment="Stretch" />

                    <!-- Right: commands for selected desktop -->
                    <ListView x:Name="CommandListView" Grid.Column="2"
                              MouseDoubleClick="OnCommandDoubleClick"
                              Margin="8,0,0,0"
                              HorizontalContentAlignment="Stretch">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Title" Width="120"
                                                DisplayMemberBinding="{Binding Title}" />
                                <GridViewColumn Header="Command Line" Width="300"
                                                DisplayMemberBinding="{Binding CommandLine}" />
                                <GridViewColumn Header="Working Directory" Width="150"
                                                DisplayMemberBinding="{Binding WorkingDirectory}" />
                                <GridViewColumn Header="Console" Width="60"
                                                DisplayMemberBinding="{Binding ShowWindowText}" />
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="3"
                                Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="+ Add Command" Click="OnAddCommandClick" />
                        <Button Content="Remove" Click="OnRemoveCommandClick" Margin="8,0,0,0" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
